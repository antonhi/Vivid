import 'package:flutter/material.dart';

import '../../configuration/colors/background_color_configuration.dart';

class AuthenticationRegisterPage extends StatelessWidget {
  const AuthenticationRegisterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: BackgroundColorConfiguration.darkModePrimary,
      body: Container(),
    );
  }
}
